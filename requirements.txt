numpy
scikit-learn
torch
tqdm
joblib
pymatgen
matminer
pandas
# Already an installed transitive dependency of pymatgen (>=1.13.0); listed
# explicitly because utils/structure.py now imports it directly (periodic
# Voronoi interstitial-void search).
scipy

# Charge-neutrality / electronegativity backend (smact_charge / pauling_en filters).
# Imported lazily — only needed when a config enables those constraints.
smact

# Optional for hyperparameter optimization (scripts/hpo.py) and the BO baseline
# (scripts/baselines/run_bo.py):
optuna>=3.0

# Optional for the GA baseline (scripts/baselines/run_ga.py):
deap

# Optional for DeepMD mode:
# ase
# deepmd-kit
